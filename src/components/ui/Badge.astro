---
interface Props {
  class?: string;
}

const { class: className = '' } = Astro.props;

const classes = `inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-primary-100 text-primary-800 ${className}`;
---

<span class={classes}>
  <slot />
</span>
